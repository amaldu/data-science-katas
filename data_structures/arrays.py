new_list = [1,2,3] # in python when we mean an array we mean a list

result = new_list[0]


if 1 in new_list:print(True)

for n in new_list:
    if n ==1:
        print(True)
        

numbers = []
len(numbers)
numbers.append(2)
numbers.append(200)

len(numbers)